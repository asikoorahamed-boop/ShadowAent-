workflows:
  extract-and-build-apk:
    name: Build Flawless Signed Release APK - ShadowNet Pro
    max_build_duration: 60
    instance_type: mac_mini_m2
    environment:
      java: 21
      vars:
        PACKAGE_NAME: "com.shadownet.app"
        APP_NAME: "ShadowNet Pro"
        NEXT_TELEMETRY_DISABLED: "1"
        LC_ALL: "en_US.UTF-8"
        LANG: "en_US.UTF-8"
        NODE_OPTIONS: "--max-old-space-size=4096"
    scripts:
      - name: Environment Information
        script: |
          node -v
          npm -v
          java -version
      - name: Extract Project ZIP
        script: |
          echo "Extracting project ZIP..."
          unzip -o project.zip -d extracted_app
          cd extracted_app
          if [ -d "ShadowAgent" ]; then
            mv ShadowAgent/* .
          fi
      - name: Clean Dependencies & Cache
        script: |
          cd extracted_app
          rm -rf node_modules package-lock.json .next out android
          npm install --legacy-peer-deps
      - name: Fix Source Code Directives & Static Export
        script: |
          cd extracted_app
          
          # Fix 'use server' directives safely across all files
          node -e '
            const fs = require("fs");
            const path = require("path");
            function processDir(dir) {
              const entries = fs.readdirSync(dir, { withFileTypes: true });
              for (let entry of entries) {
                const fullPath = path.join(dir, entry.name);
                if (entry.isDirectory()) {
                  if (entry.name !== "node_modules" && entry.name !== ".next") processDir(fullPath);
                } else if (/\.(js|jsx|ts|tsx)$/.test(entry.name)) {
                  let content = fs.readFileSync(fullPath, "utf8");
                  if (content.includes("use server")) {
                    content = content.replace(/["\x27]use server["\x27]/g, "// use server");
                    fs.writeFileSync(fullPath, content, "utf8");
                  }
                }
              }
            }
            processDir(".");
          '

          # Fix calendar component UI type check
          node -e '
            const fs = require("fs");
            const file = "src/components/ui/calendar.tsx";
            if (fs.existsSync(file)) {
              let c = fs.readFileSync(file, "utf8");
              c = c.replace(/IconLeft:/g, "// IconLeft:").replace(/IconRight:/g, "// IconRight:");
              fs.writeFileSync(file, c, "utf8");
            }
          '

          # Create Next.js Export Configuration
          cat << 'EOF' > next.config.mjs
          /** @type {import('next').NextConfig} */
          const nextConfig = {
            output: 'export',
            images: { unoptimized: true },
            typescript: { ignoreBuildErrors: true },
            eslint: { ignoreDuringBuilds: true },
            webpack: (config, { isServer, webpack }) => {
              if (!isServer) {
                config.resolve.fallback = {
                  ...config.resolve.fallback,
                  perf_hooks: false,
                  dns: false, net: false, tls: false, fs: false,
                  child_process: false, async_hooks: false, http2: false,
                  http: false, https: false, crypto: false, stream: false,
                  os: false, zlib: false, path: false, url: false,
                  v8: false, vm: false, tty: false, readline: false,
                };
                config.plugins.push(
                  new webpack.NormalModuleReplacementPlugin(/^node:/, (resource) => {
                    resource.request = resource.request.replace(/^node:/, '');
                  })
                );
              }
              return config;
            },
          };
          export default nextConfig;
          EOF

          # Run Next Static Export
          rm -rf .next out
          npx next build
      - name: Generate Fresh Android Native Shell & Permissions
        script: |
          cd extracted_app
          
          # Initialize fresh capacitor config with new identity
          node -e '
            const fs = require("fs");
            const cfg = {
              appId: process.env.PACKAGE_NAME,
              appName: process.env.APP_NAME,
              webDir: "out",
              server: {
                androidScheme: "https"
              }
            };
            fs.writeFileSync("capacitor.config.json", JSON.stringify(cfg, null, 2));
            if (fs.existsSync("capacitor.config.ts")) fs.unlinkSync("capacitor.config.ts");
          '
          
          # Add native Android platform
          npx cap add android
          npx cap sync android
          
          # Configure AndroidManifest.xml for full compatibility & multi-device use
          node -e '
            const fs = require("fs");
            const manifestPath = "android/app/src/main/AndroidManifest.xml";
            if (fs.existsSync(manifestPath)) {
              let m = fs.readFileSync(manifestPath, "utf8");
              const perms = `
                <uses-permission android:name="android.permission.INTERNET" />
                <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
                <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
              `;
              m = m.replace("<application", perms + "\n    <application");
              m = m.replace("android:hardwareAccelerated=\"true\"", "android:hardwareAccelerated=\"true\" android:exported=\"true\"");
              fs.writeFileSync(manifestPath, m, "utf8");
            }
          '
      - name: Sign & Build Production Release APK
        script: |
          cd extracted_app/android
          chmod +x gradlew
          
          # Generate Dedicated Production Release Keystore
          keytool -genkey -v -keystore app/release.keystore -storepass mypassword123 -alias mykeyalias -keypass mypassword123 -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=ShadowNet, O=Production, C=US" || true
          
          # Inject V1 + V2 Signing Certificate into Gradle
          node -e '
            const fs = require("fs");
            const gradlePath = "app/build.gradle";
            if (fs.existsSync(gradlePath)) {
              let g = fs.readFileSync(gradlePath, "utf8");
              const signingConfig = `
                signingConfigs {
                    release {
                        storeFile file("release.keystore")
                        storePassword "mypassword123"
                        keyAlias "mykeyalias"
                        keyPassword "mypassword123"
                        v1SigningEnabled true
                        v2SigningEnabled true
                    }
                }
              `;
              g = g.replace(/android\s*\{/, "android {\n" + signingConfig);
              g = g.replace(/buildTypes\s*\{\s*release\s*\{/, "buildTypes {\n        release {\n            signingConfig signingConfigs.release\n");
              fs.writeFileSync(gradlePath, g, "utf8");
            }
          '
          
          # Build signed APK
          ./gradlew clean assembleRelease -Dorg.gradle.java.home=$JAVA_HOME
    artifacts:
      - extracted_app/android/app/build/outputs/apk/release/*.apk